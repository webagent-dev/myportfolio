import React from 'react'
import { CircleContainer } from './circle.style'
function Circle({top, right, left }) {
  return (
   <CircleContainer top={top} right={right} left={left}>

   </CircleContainer>
  )
}

export default Circle